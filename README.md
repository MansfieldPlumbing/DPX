# DPX
DPX is a high-performance, push-based dataflow inference pipeline built on first principles. Bypassing pull-based graph orchestration, DPX relies on lock-free, double-buffered node isolation to decouple memory lifecycles and maximize hardware-accelerated throughput.
